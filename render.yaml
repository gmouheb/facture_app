services:
  - type: web
    name: flask-invoice-app
    env: python
    rootDir: .  # This tells Render to use the repository root
    buildCommand: chmod +x build.sh && ./build.sh && pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
